export class ExportUtils {
  /**
   * 导出Excel文件
   * @param data
   * @param headers
   * @param fileName
   * @returns 文件路径
   */
  public static async exportToExcel(data: any[][], headers: string[], fileName: string): Promise<string> {
    // TODO: 实现Excel导出功能
    return '';
  }
}
